# ZeroMQ benchmark

Use `scripts/benchmark_zeromq.py` against a running `worker.py` instance. The default campaign is 4 sizes × 2 dtypes × 10 measured repetitions, with 2 warm-ups. Raw results belong in `results/zeromq/` and must remain separate from Vercel data.