name: Benchmark validation

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Upgrade pip
        run: python -m pip install --upgrade pip
      - name: Install dependencies
        run: python -m pip install -r requirements.txt
      - name: Verify dependency consistency
        run: python -m pip check
      - name: Start worker
        run: |
          python worker.py --host 127.0.0.1 --port 5555 > worker.log 2>&1 &
          echo $! > worker.pid
          sleep 2
      - name: Run reproducible smoke benchmark
        run: |
          mkdir -p artifacts
          python orchestrator.py \
            --worker tcp://127.0.0.1:5555 \
            --sizes 64 128 256 \
            --repetitions 3 \
            --warmup-runs 1 \
            --results artifacts/benchmark-results.csv \
            --metadata artifacts/benchmark-metadata.json
      - name: Validate benchmark output
        run: |
          python - <<'PY'
          import csv
          import json
          from pathlib import Path

          path = Path("artifacts/benchmark-results.csv")
          metadata_path = Path("artifacts/benchmark-metadata.json")
          rows = list(csv.DictReader(path.open(encoding="utf-8")))
          metadata = json.loads(metadata_path.read_text(encoding="utf-8"))
          assert len(rows) == 9, rows
          assert all(row["status"] == "MEASURED" for row in rows), rows
          assert all(row["error"] in ("", None) for row in rows), rows
          assert [int(row["n"]) for row in rows] == [64, 64, 64, 128, 128, 128, 256, 256, 256]
          assert metadata["repetitions"] == 3
          assert metadata["warmup_runs"] == 1
          assert metadata["sizes"] == [64, 128, 256]
          assert metadata["blas_runtime"]
          print(f"Validated {len(rows)} measured benchmark rows")
          PY
      - name: Stop worker
        if: always()
        run: |
          if [ -f worker.pid ]; then kill "$(cat worker.pid)" || true; fi
      - name: Upload benchmark artifact
        uses: actions/upload-artifact@v4
        with:
          name: neuro-k-benchmark
          path: |
            artifacts/benchmark-results.csv
            artifacts/benchmark-metadata.json
            worker.log
